# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'
use_frameworks!

target 'Moobeez' do

  # Pods for Moobeez
  pod 'SDWebImage', '~> 4.0'
  pod 'youtube-ios-player-helper', '~> 0.1.4'
  pod 'SwiftyBeaver'
  
end

target 'Today' do
    
    # Pods for Today
    pod 'SDWebImage', '~> 4.0'
    pod 'SwiftyBeaver'
end

target 'Missed Shows' do
    
    # Pods for Today
    pod 'SDWebImage', '~> 4.0'
    pod 'SwiftyBeaver'
end
